name: test

on:
  push:
    branches:
      - master

jobs:
  draft-release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Get result
        id: compareresult
        run: echo "::set-output result=true"
      - uses: ./
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          COMPARE-RESULT: ${{ steps.compareresult.outputs.result }}
